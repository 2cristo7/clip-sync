# clipsync-platform

Per-OS desktop integration adapters: tray icon, system notifications,
autostart and global hotkeys, each gated behind a `cfg(target_os = ...)`
once implemented. Stubbed during Phase 1.9 — real adapters land
alongside the desktop app shells in Plans 2/3.
